# TRABAJO FINAL PRGRAMACION 3 TUP

## Estructura del Proyecto

```
├── public/
│   └── index.html          # Archivo HTML principal
├── src/
│   ├── index.js            # Punto de entrada de React
│   ├── index.css           # Estilos globales
│   ├── App.js              # Componente principal
│   └── App.css             # Estilos del componente App
├── package.json            # Dependencias del proyecto
└── .gitignore              # Archivos a ignorar en Git
```

## Instalación

1. Navega a la carpeta del proyecto:
```bash
cd TPIP3
```

2. Instala las dependencias:
```bash
npm install
```

## Ejecución

Para iniciar el servidor de desarrollo:

```bash
npm start
```

La aplicación se abrirá en [http://localhost:3000](http://localhost:3000)

## Compilación para Producción

Para compilar la aplicación para producción:

```bash
npm build
```

## Personalización

- Modifica `src/App.js` para cambiar el contenido
- Edita `src/App.css` para cambiar los estilos
- Agrega nuevos componentes en la carpeta `src/`

## Próximos Pasos

1. Instala las dependencias con `npm install`
2. Inicia el servidor con `npm start`
3. Comienza a desarrollar tu página

## Integrantes
 - Ayquipa Marco
 - Dino Gabriel
 - Paletta Jeremias
 - Sione Matias
